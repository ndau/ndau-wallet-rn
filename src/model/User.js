const User = function() {
  this.date = Date.now();
  (this.setupStep = 'ndau.SetupMain'),
    (this.userToken = ''),
    (this.password = ''),
    (this.twelveWorldPhrase = ''),
    (this.eaiNode = '');
};

module.exports = User;
